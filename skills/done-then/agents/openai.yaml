interface:
  display_name: "Done Then"
  short_description: "Gate a post-task action safely"
  default_prompt: "Use $done-then to arm a dry-run for this task, then report the final DoneThen state without invoking a power action."
